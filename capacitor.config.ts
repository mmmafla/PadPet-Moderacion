import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PadPet - Administracion',
  webDir: 'www'
};

export default config;
